{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://intentusnet.com/schema/emcl_envelope.json",
  "title": "EMCLEnvelope",
  "type": "object",
  "additionalProperties": true,
  "required": ["emclVersion", "ciphertext", "nonce", "hmac"],
  "properties": {
    "emclVersion": {
      "type": "string",
      "description": "Version of the EMCL envelope specification."
    },
    "ciphertext": {
      "type": "string",
      "description": "Encrypted data (typically base64 or encoded JSON)."
    },
    "nonce": {
      "type": "string",
      "description": "Nonce/IV used for encryption or signing."
    },
    "hmac": {
      "type": "string",
      "description": "Authentication tag or MAC for integrity verification."
    },
    "identityChain": {
      "type": "array",
      "description": "Optional chain of agent identities for audit / provenance.",
      "items": {
        "type": "string"
      }
    }
  }
}
