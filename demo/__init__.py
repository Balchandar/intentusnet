"""IntentusNet end-to-end demo package.

The demo layer sits on top of the production kernel in
``src/intentusnet/security/`` and ``src/intentusnet/wal/``. It does not
re-implement any core logic — it only wires existing primitives into a
narrative that contrasts blind execution with observable, replayable AI
execution.
"""
